import styled from "styled-components"

const Code = styled.code`
  position: relative;
  display: inline-block;
  background: var(--bg-snippet) !important;
  padding: 0.4rem 0.5rem;
  font-family: inherit;
  color: inherit;
  border-radius: var(--grvsc-border-radius);
`

export default Code